package com.cyfqz.dstream.transformation

/**
 * 1、访问event本身数据(比如：Event的时间，Event的当前的key等
 * 2、管理状态 state(仅在keyed stream中)
 * 3、管理定时器 Timer(包括:注册定时器,删除定时器，触发定时器)
 * 总而言之，ProcessFunction 是Flink 最底层的API，也是功能最强大的
 */
object TestProcessFunction {

  def main(args: Array[String]): Unit = {

  }

}
